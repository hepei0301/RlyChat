import IM, { openIM, toggleHorn, closeIM } from './src/pages/im/index';
import Meet, { openMeet, closeMeet } from './src/pages/meet';
import Totalkd, { setChannelId } from './src/pages/totalkd';

export { IM, Meet, openIM, openMeet, toggleHorn, closeMeet, closeIM, Totalkd, setChannelId };
